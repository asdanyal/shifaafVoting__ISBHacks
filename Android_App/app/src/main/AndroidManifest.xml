<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nabihaiqbal.evoting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splashscreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CNIC"
            android:label="@string/app_name" />
        <activity
            android:name=".code"
            android:label="@string/title_activity_code"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Finger_verification"
            android:label="@string/title_activity_finger_verification"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".verification_activity"
            android:label="@string/title_activity_verification_activity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".selection"
            android:label="@string/title_activity_selection"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".final_vote"
            android:label="@string/title_activity_final_vote"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>
